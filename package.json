{
  "name": "ecommerce-app",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix front-end\"",
    "dev:api": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix front-end",
    "install:all": "npm install --prefix server && npm install --prefix front-end"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
